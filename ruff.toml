# Ruff 静态检查配置：显式固定规则集，避免随 ruff 版本默认规则漂移导致 CI 抖动。
# 此处使用经典默认集（E4/E7/E9/F：语法、未定义名、未使用等真实问题）。
# 安全类规则（B608 等）由 bandit 单独把关（见 .github/workflows/ci.yml），
# 宽泛异常捕获（BLE001/S110/S112）是流量解析代码对畸形数据的刻意容错，不在本门禁内。
target-version = "py311"

[lint]
select = ["E4", "E7", "E9", "F"]
